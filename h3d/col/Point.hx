package h3d.col;

/**
 * 3D 点类型
 *
 * 是 `h3d.Vector` 的类型别名，在碰撞检测系统中使用。
 * 与 Vector 完全一致，但语义上表示"点"（位置）而非"向量"。
 *
 * 在碰撞检测中，点被用于：
 * - 表示碰撞体上的最近点
 * - 射线上的点
 * - 球体/包围盒的测试点
 */
typedef Point = h3d.Vector;